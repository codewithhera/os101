[package]
name = "calculator"
version = "0.1.0"
edition = "2021"

[dependencies]
os101-user = { path = "../../os101-user" }
linked_list_allocator = "0.10"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
