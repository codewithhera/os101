/* `struct timeval` and `gettimeofday` live in <time.h> here; this header
   exists because that is where portable code looks for them. */
#ifndef _OS101_SYS_TIME_H
#define _OS101_SYS_TIME_H

#include <time.h>

#endif /* _OS101_SYS_TIME_H */
