[package]
name = "os101-user"
version = "0.0.1"
edition = "2021"
description = "OS101 userspace demo crate (Phase 9)."

[lib]
crate-type = ["rlib"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
